import { OnInit, TemplateRef } from '@angular/core';
declare type ThyAlertType = 'success' | 'warning' | 'danger' | 'info' | 'primary-week' | 'success-week' | 'warning-week' | 'danger-week' | 'primary-weak' | 'success-weak' | 'warning-weak' | 'danger-weak';
export declare class ThyAlertComponent implements OnInit {
    class: string;
    messageTemplate: TemplateRef<HTMLElement>;
    messageText: string;
    set thyType(value: ThyAlertType);
    set thyMessage(value: string | TemplateRef<HTMLElement>);
    set thyIcon(value: boolean | string);
    get thyIcon(): boolean | string;
    thyCloseable: boolean;
    alertOperation: TemplateRef<any>;
    private _typeIcon;
    private _showIcon;
    private _icon;
    private _type;
    constructor();
    ngOnInit(): void;
    closeAlert(): void;
}
export {};
