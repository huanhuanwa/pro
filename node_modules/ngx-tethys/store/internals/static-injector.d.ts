import { Injector } from '@angular/core';
export declare function setInjector(parentInjector: Injector): void;
/**
 * Ensure that we don't keep any references in case of the bootstrapped
 * module is destroyed via `NgModuleRef.destroy()`.
 */
export declare function clearInjector(): void;
export declare function getInjector(): Injector;
