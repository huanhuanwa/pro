import { InjectionToken } from '@angular/core';
import { ScrollStrategy } from '@angular/cdk/overlay';
import { ThyAbstractOverlayConfig, ThyUpperOverlayPosition } from 'ngx-tethys/core';
/** Valid ARIA roles for a dialog element. */
export declare type ThyDialogRole = 'dialog' | 'alertdialog';
export declare enum ThyDialogSizes {
    lg = "lg",
    supperLg = "supper-lg",
    maxLg = "max-lg",
    md = "md",
    sm = "sm",
    full = "full"
}
/**
 * Configuration for opening a modal dialog with the ThyDialog service.
 */
export declare class ThyDialogConfig<TData = any> extends ThyAbstractOverlayConfig<TData> {
    /** The ARIA role of the dialog element. */
    role?: ThyDialogRole;
    /** Position overrides. */
    position?: ThyUpperOverlayPosition;
    /** Dialog size md, lg, sm*/
    size?: ThyDialogSizes;
    /** Scroll strategy to be used for the dialog. */
    scrollStrategy?: ScrollStrategy;
}
/** Injection token that can be used to specify default dialog options. */
export declare const THY_DIALOG_DEFAULT_OPTIONS: InjectionToken<ThyDialogConfig<any>>;
export declare const THY_DIALOG_DEFAULT_OPTIONS_PROVIDER: {
    provide: InjectionToken<ThyDialogConfig<any>>;
    useValue: {
        role: string;
        hasBackdrop: boolean;
        backdropClass: string;
        panelClass: string;
        backdropClosable: boolean;
        closeOnNavigation: boolean;
        autoFocus: boolean;
        restoreFocus: boolean;
    };
};
export declare type ThyDialogFooterAlign = 'left' | 'right' | 'center';
export declare class ThyDialogLayoutConfig {
    footerAlign?: ThyDialogFooterAlign;
    footerDivided?: boolean;
}
export declare const THY_DIALOG_LAYOUT_CONFIG: InjectionToken<ThyDialogLayoutConfig>;
export declare const THY_DIALOG_LAYOUT_CONFIG_PROVIDER: {
    provide: InjectionToken<ThyDialogLayoutConfig>;
    useValue: {
        footerAlign: string;
        footerDivided: boolean;
    };
};
