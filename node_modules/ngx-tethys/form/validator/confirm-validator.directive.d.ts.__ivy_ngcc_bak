import { AbstractControl, Validator, ValidatorFn } from '@angular/forms';
export declare function confirmValidator(value: string): ValidatorFn;
export declare class ThyConfirmValidatorDirective implements Validator {
    confirm: string;
    validate(control: AbstractControl): import("@angular/forms").ValidationErrors;
}
