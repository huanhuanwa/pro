import { Tree } from '@angular-devkit/schematics';
import { ContentChange } from '../types';
export declare class UpdateFileService {
    private tree;
    constructor(tree: Tree);
    private update;
    private remove;
    private replace;
    change(filePath: string, changeList: ContentChange[]): void;
}
export declare function createUpdateFileService(tree: any): UpdateFileService;
