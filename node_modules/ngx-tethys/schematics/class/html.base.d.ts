import { CssSelectorForHtml } from 'cyia-code-util';
import { UpdateFileService } from '../utils';
export declare abstract class HtmlMigrationBase {
    protected filePath: string;
    protected startOffset: number;
    protected instance: CssSelectorForHtml;
    protected updateFileService: UpdateFileService;
    constructor(filePath: string, startOffset: number, instance: CssSelectorForHtml, updateFileService: UpdateFileService);
    abstract run(): void;
}
