import { MigrationBase } from './base';
import { ContentChange } from '../types';
export declare abstract class ImportNameChangeBase extends MigrationBase {
    private changeNodeMap;
    abstract readonly relation: Record<string, string>;
    run(): void;
    changeRelationIdentifier(oldNamed: string, newNamed: string, list: ContentChange[]): void;
}
