import { ContentChange } from './content-change';
export declare class ReplaceContentChange extends ContentChange {
    length: number;
    content: string;
    constructor(pos: number, length: number, content: string);
}
