import { SchematicContext, Tree, Rule } from '@angular-devkit/schematics';
interface NgAddSchema {
    project?: string;
    icon?: boolean;
    animations?: boolean;
}
export declare function main(options?: NgAddSchema): (host: Tree, context: SchematicContext) => Promise<Rule>;
export {};
