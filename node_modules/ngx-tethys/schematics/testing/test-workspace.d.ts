/// <reference types="node" />
import { SchematicTestRunner, UnitTestTree } from '@angular-devkit/schematics/testing';
export declare class TestWorkspaceFactory {
    runner: SchematicTestRunner;
    private hostTree;
    private tree;
    constructor(runner: SchematicTestRunner);
    create(options?: {
        name?: string;
        newProjectRoot?: string;
        version?: string;
        minimal?: boolean;
        strict?: boolean;
        packageManager?: 'npm' | 'yarn' | 'pnpm' | 'cnpm';
    }): Promise<UnitTestTree>;
    addApplication(options: {
        name: string;
        [name: string]: any;
    }): Promise<UnitTestTree>;
    addLibrary(options: {
        name: string;
        [name: string]: any;
    }): Promise<UnitTestTree>;
    addNewFile(templatePath: string, content: string | Buffer): UnitTestTree;
    /**
     * @description 复制硬盘中的资源到虚拟的tree中,目前并没用用到,因为换了一种方案,但是好不容易写的就不删了
     * @param from 真实路径
     * @param to 虚拟路径
     */
    copyFolder(from: string, to: string, overwrite?: boolean): UnitTestTree;
    getTree(): UnitTestTree;
}
export declare function createTestWorkspaceFactory(runner: SchematicTestRunner): TestWorkspaceFactory;
